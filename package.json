{
  "name": "PrescriptText",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter wasm run build && vite",
    "build": "pnpm --filter wasm run build && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vite-plugin-solid": "^2.11.10"
  },
  "dependencies": {
    "solid-js": "^1.9.11"
  }
}
